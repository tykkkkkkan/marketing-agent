# ⚠️ 配置统一放在项目根目录，不要在这里建 .env
#
# 唯一权威模板：  ../.env.example      （即 marketing-agent/.env.example）
# 你的本地配置：  ../.env              （即 marketing-agent/.env，已被 .gitignore 忽略）
#
# 加载顺序（backend/db.py 与 backend/zt_client.py 一致）：
#   backend/.env → marketing-agent/.env → ../ZT-agent/.env
# 三者都不存在时才会落到代码内置默认值。
#
# 需要配置的项：数据库(DB_*) / DeepSeek(DEEPSEEK_*) /
#               运营执行层(ZT_AGENT_BASE_URL, ZT_STAFF_USER, ZT_STAFF_PASSWORD,
#                          MKT_EXECUTE_MODE, ZT_AGENT_TIMEOUT)

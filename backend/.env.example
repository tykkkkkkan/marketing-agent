# ── 复用 ZT-agent 的 MySQL（只读 agent_db，业务表不写）──
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_NAME=agent_db
DB_PASSWORD=

# ── 复用 DeepSeek 密钥（第3步 LangChain 接同一模型）──
DEEPSEEK_API_KEY=

# ── 营销 Agent 服务端口 ──
PORT=8001
